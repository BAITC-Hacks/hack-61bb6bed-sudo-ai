POSTGRES_USER=sana
POSTGRES_PASSWORD=sana_local_only
POSTGRES_DB=sana
API_PORT=8000
APP_ENV=development
DEMO_AUTH_ENABLED=false
FRONTEND_URL=http://localhost:5173
# Empty key/model keeps the explicitly labelled local fallback available.
OPENAI_API_KEY=
OPENAI_MODEL=
AI_TIMEOUT_SECONDS=25

# Minimal local dataset for reviewers; ignored outside development.
REVIEW_DEMO_ENABLED=true
